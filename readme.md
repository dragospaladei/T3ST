# T3ST – Simulation Framework

T3ST is a simulation framework with a GUI-driven parameter setup, Fortran-based execution, and optional post-processing using Mathematica. This document describes the required folder structure and the recommended workflow.

---

## 📁 Directory Structure

T3ST expects the following directory layout:

```
Parent_Folder/
├── config/                         # GUI configuration files
│   ├── parameters.yaml
│   └── scenarios.yaml
│
├── data/                           # Simulation output data
│   ├── Sim_01/
│   │   ├── Run_00001/
│   │   ├── Run_00002/
│   │   └── ...
│   ├── Sim_02/
│   │   └── ...
│   └── ...
│
├── G_EDSQK/                        # Optional: magnetic equilibrium data
│   ├── Documentation/             # Reference material
│   ├── MAST-U/                     # MAST-U discharges
│   ├── TCV/                        # TCV discharges
│   └── WEST/                       # WEST discharges
│
├── Mathematica/                    # Optional but recommended
│   └── Process_data.nb             # Mathematica notebook for data processing & visualization
│
├── Sims/                           # Simulation parameter files (generated by GUI)
│   ├── Sim_01.dat
│   ├── Sim_02.dat
│   └── ...
│
├── Source/                         # Fortran source code
│   ├── compile
│   ├── set_ifx
│   ├── run_all.sh
│   └── *.f90
│
└── T3ST_GUI.py                     # Graphical User Interface
```

## 🧠 Conceptual Model

1. Parameter Schema (parameters.yaml)
2. Scenarios (scenarios.yaml)
3. Databases (databases.yaml)
4. Selections (selections.yaml)

---

## 🔪 Slice System

Free parameters:
method: slice
free_parameters: ["Ln"]

Fixed values:
method: slice
fixed_values:
  Phi: 0.01

---


### Notes
- A **Simulation** (`Sim_XX`) corresponds to a specific parameter scan (potentially many-dimensional, up to 3 dimensions).
- Each **Run** (`Run_000XX`) represents one discrete set of parameters.
- The `G_EDSQK` and `Mathematica` directories are optional but highly recommended (without the first you can't do realistic equilibria, the second is an in-place setup for post-processing and visualization;)
- Python-based post-processing may be added in the future.

---

## ▶️ Workflow

### 1. Launch the GUI

From the **Parent_Folder**, run:

```bash
python3 T3ST_GUI.py
```

> On Windows, double-clicking the file also works.

---

### 2. Configure the Simulation (GUI)

In the GUI:

1. Select the **simulation number**   [note that preexisting simulation data will be overwritten if you perform simulations on top]
2. Choose a **scenario**              [Famous, predefined scenarious are in place;]
3. Manually adjust parameters if needed
4. Select:
   - The parameter to vary
   - Number of discrete values (number of runs)
   - Parameter interval (initial → final)
5. Export the parameters              [nottom right button]

This generates the corresponding `Sim_XX.dat` file in the `Sims/` directory.

---

### 3. Compile and Run the Code

Navigate to the source directory:

```bash
cd Source
```

Then:

```bash
source set_ifx
source compile
```

or alternatively:

```bash
make
```

Run the executable:

```bash
./a.out
```

When prompted:
- Enter the **simulation number** (`noofsim`)

#### Batch / Meta-Simulations

To run multiple simulations automatically:

```bash
./run_all.sh
```

---

### 4. Data Output

Simulation results are written to:

```
Parent_Folder/data/Sim_<noofsim>/Run_000XX/
```

Each `Run_000XX` corresponds to one parameter set.

---

### 5. Post-Processing

Data processing and visualization can be performed using Mathematica:

- Open:
  ```
  Mathematic/Process_data.nb
  ```
- Point it to the desired `Sim_XX/Run_000XX` directory

---

## 🧩 Future Extensions

- Python-based data processing and visualization
- Additional equilibrium databases
- Expanded GUI automation

